// Headless batch runner: N seeded games in parallel, emitting CSV/JSON for balance analysis.
// Implemented in Phase 3; this entry point exists so the skeleton builds and runs.

Console.WriteLine("Shapes — simulation runner");
Console.WriteLine("Not yet implemented (see PLAN.md, Phase 3).");
